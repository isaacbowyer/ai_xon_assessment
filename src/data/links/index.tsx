import type { INavLink } from "../../interfaces/INavLink";

const NAV_LINKS: INavLink[] = [
  { href: "/", label: "UPCOMING" },
  { href: "/past", label: "PAST" },
];

export const LINKS = {
  NAV_BAR: NAV_LINKS,
};
