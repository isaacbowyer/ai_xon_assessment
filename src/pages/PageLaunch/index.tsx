import { CustomTitle } from "../../components/atoms/CustomTitle";
import { PublicTemplate } from "../../components/templates/PublicTemplate";

export const PageLaunch = () => {
  return <PublicTemplate main={<CustomTitle title="UPCOMING LAUNCHES" />} />;
};
